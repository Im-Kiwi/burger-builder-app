import { makeStyles } from '@mui/styles'

export const styles = makeStyles({
    emptyCart : {
        marginTop : 120,
        width : '100%',
        position : 'relative'
    },
    cart : {
        position : 'absolute',
        marginTop : 120,  
        height: '90%', 
        overflowY : 'auto'
    }
})