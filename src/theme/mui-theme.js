import { createTheme } from '@mui/material/styles'

export const userFormTheme = createTheme({
    palette : {
        mode : 'dark',
        common : {
            white : '#f9b826'
        }
    }
})

